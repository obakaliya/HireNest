package com.api.controller;

import org.springframework.stereotype.Controller;

@Controller
public class JobApplicationController {
  /**
   * Apply for the job with resume (resume should be store in s3, for a local test store it under a
   * project folder) View the submitted applications
   */
}
